import { styled, Input as TInput } from 'tamagui';

export const Input = styled(TInput, {
  w: '100%',
  h: '$4',
  m: '$0',
  mb: '$2',
  borderWidth: '$1',
  borderColor: '$green8',
  color: '$green12',
});
